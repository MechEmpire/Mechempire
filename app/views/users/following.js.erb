$('#following').on('ajax:success', function(event, xhr, status, error) {
  $(this).html("取消关注");
  $(this).attr("href", $(this).attr("href").replace("/following","/unfollowing"));
  // console.log(Number($("#follower").html()));
});
